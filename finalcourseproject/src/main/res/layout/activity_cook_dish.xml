<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    xmlns:tools="http://schemas.android.com/tools">

    <Toolbar
        android:id="@+id/toolbarCookDish"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorRed"
        android:title="@string/toolbar_text_cook_dish"
        android:titleTextColor="@color/colorWhite" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorRed"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/viewTextViewStepsCount"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            tools:text="Steps: "
            android:textColor="@color/colorWhite"
            android:textSize="@dimen/default_title_text_size"
            android:padding="@dimen/default_padding" />

        <TextView
            android:id="@+id/viewTextViewTimeCount"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            tools:text="Time Left: "
            android:textColor="@color/colorWhite"
            android:textSize="@dimen/default_title_text_size"
            android:padding="@dimen/default_padding" />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerListToDoCheckBox"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/default_padding" />

</LinearLayout>